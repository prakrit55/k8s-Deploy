apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-K8sDeploy-App.fullname" . }}-test-connection"
  labels:
    {{- include "my-K8sDeploy-App.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-K8sDeploy-App.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
